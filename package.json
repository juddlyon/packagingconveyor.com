{
  "name": "packagingconveyor-tmp",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postdeploy:indexnow": "node scripts/indexnow.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "npx playwright test",
    "test:a11y": "npx playwright test tests/a11y.spec.ts",
    "test:e2e": "npx playwright test tests/e2e.spec.ts",
    "deploy": "astro build && netlify deploy --prod --dir=dist"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^6.0.4",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.1",
    "@playwright/test": "^1.58.2"
  }
}
